rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write for all users (no authentication required)
    match /customers/{document=**} {
      allow read, write: if true;
    }
    match /collections/{document=**} {
      allow read, write: if true;
    }
  }
}
